{
  "name": "@bugpulse/mcp",
  "version": "0.1.0",
  "description": "MCP server for BugPulse — query bug reports from AI agents",
  "bin": {
    "bugpulse-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "tsc"
  },
  "files": ["dist/"],
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
